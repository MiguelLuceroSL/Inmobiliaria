@* @model IEnumerable<Inmobiliaria.Models.Inquilino>

@{
    ViewData["Title"] = "Inquilinos";
}
@if (TempData["SuccessMessage"] != null)
{
    <div class="alert alert-success">
        @TempData["SuccessMessage"]
    </div>
}
@if (TempData["ErrorMessage"] != null)
{
    <div class="alert alert-danger">
        @TempData["ErrorMessage"]
    </div>
}


<h1>
	Inquilinos
	<div class="btn-group pull-right">
		<a class="btn btn-primary" asp-action="Create" title="Crear"><i class="fa-solid fa-plus">
            <h4>+</h4>  
        </i></a>
	</div>
</h1>
<table border="4" cellpadding="5" class="table table-striped">
    <tr>
        <th>DNI</th>
        <th>Apellido</th>
        <th>Nombre</th>
        <th>Email</th>
        <th>Teléfono</th>
        <th>Domicilio</th>
        <th>Acciones</th>
    </tr>
    @foreach (var p in Model)
    {
        <tr>
            <td>@p.dniInquilino</td>
            <td>@p.apellido</td>
            <td>@p.nombre</td>
            <td>@p.email</td>
            <td>@p.telefono</td>
            <td>@p.domicilioPersonal</td>
            <td>
            <a asp-action="Edit" asp-route-id="@p.idInquilino" class="btn btn-warning">Editar</a> |
            <a asp-action="Delete" asp-route-id="@p.idInquilino" class="btn btn-danger">Eliminar</a>
            </td>
        </tr>
    }
</table> *@
@model IEnumerable<Inmobiliaria.Models.Inquilino>

@{
    ViewData["Title"] = "Inquilinos";
    int pagina = ViewBag.Pagina ?? 1;
    int totalPaginas = ViewBag.TotalPaginas ?? 1;
}

@if (TempData["SuccessMessage"] != null)
{
    <div class="alert alert-success">
        @TempData["SuccessMessage"]
    </div>
}
@if (TempData["ErrorMessage"] != null)
{
    <div class="alert alert-danger">
        @TempData["ErrorMessage"]
    </div>
}

<h1>
    Inquilinos
    <div class="btn-group pull-right">
        <a class="btn btn-primary" asp-action="Create" title="Crear">
            <i class="fa-solid fa-plus"><h4>+</h4></i>
        </a>
    </div>
</h1>

<table border="4" cellpadding="5" class="table table-striped">
    <thead>
        <tr>
            <th>DNI</th>
            <th>Apellido</th>
            <th>Nombre</th>
            <th>Teléfono</th>
            <th>Email</th>
            <th>Domicilio</th>
            <th>Acciones</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var i in Model)
        {
            <tr>
                <td>@i.dniInquilino</td>
                <td>@i.apellido</td>
                <td>@i.nombre</td>
                <td>@i.telefono</td>
                <td>@i.email</td>
                <td>@i.domicilioPersonal</td>
                <td>
                    <a asp-action="Edit" asp-route-id="@i.idInquilino" class="btn btn-warning">Editar</a>|
                    <a asp-action="Delete" asp-route-id="@i.idInquilino" class="btn btn-danger">Eliminar</a>
                    @* <a asp-action="Details" asp-route-id="@i.idInquilino" class="btn btn-primary">Detalles</a> *@
                </td>
            </tr>
        }
    </tbody>
</table>

<!-- 🔄 Paginación -->
<nav aria-label="Paginación de inquilinos">
    <ul class="pagination justify-content-left">
        <li class="page-item @(pagina <= 1 ? "disabled" : "")">
            <a class="page-link" asp-action="Index" asp-route-pagina="@(pagina - 1)">« Anterior</a>
        </li>

        @for (int i = 1; i <= totalPaginas; i++)
        {
            <li class="page-item @(i == pagina ? "active" : "")">
                <a class="page-link" asp-action="Index" asp-route-pagina="@i">@i</a>
            </li>
        }

        <li class="page-item @(pagina >= totalPaginas ? "disabled" : "")">
            <a class="page-link" asp-action="Index" asp-route-pagina="@(pagina + 1)">Siguiente »</a>
        </li>
    </ul>
</nav>
